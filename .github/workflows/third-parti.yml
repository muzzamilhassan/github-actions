name: ASCII cowsay Action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: ru script
        run: |
            chmod +x script.sh
            ./script.sh
        
